import theme, { COLORS, SIZES, FONTS } from './theme';
import dummyData, { categoryList, categories, destinationList, ProfileData } from './dummyData';

export {
  theme,
  COLORS,
  SIZES,
  FONTS,
  dummyData,
  categoryList,
  categories,
  destinationList,
  ProfileData,
};
